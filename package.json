{
  "name": "ai-tam-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && SKIP_NEXT_APP_BUILD=1 npx opennextjs-cloudflare build",
    "start": "next start",
    "preview": "SKIP_NEXT_APP_BUILD=1 npx opennextjs-cloudflare build && wrangler dev",
    "deploy": "npx opennextjs-cloudflare build && wrangler deploy"
  },
  "dependencies": {
    "next": "^15.5.18",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@opennextjs/cloudflare": "^1.19.11",
    "esbuild": "^0.28.1",
    "eslint": "^9",
    "eslint-config-next": "^15",
    "wrangler": "^4"
  }
}
